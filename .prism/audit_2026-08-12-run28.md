# Run 28 quick audit — 2026-08-12

**Baseline pytest:** environment hit the same `_cffi_backend` /
`pyo3_runtime.PanicException` collection error 5 prior runs (5, 6, 12, 25,
and implicitly others) have already logged — fixed the same way, via
`pip install --force-reinstall cffi cryptography`. Confirmed this is an
environment cold-start quirk, not a repo regression.

**Baseline result:** `528 passed`, exactly matching Run 27's reported tip
(502 → 528 after Run 27's chi2/ANOVA power work). No drift, no
regressions carried over.

**TODO/FIXME scan:** `grep -rn "TODO\|FIXME\|XXX" modules/ app.py` — the
only `XXXX` hits are in `modules/pii_detector.py`'s masking output strings
(e.g. `"XXXX-XXXX-1234"`), not debt markers. Zero real TODO/FIXME comments
anywhere in `modules/` or `app.py`. Matches Run 27's own "no real
TODO/FIXME debt" finding — reconfirmed, not re-litigated further given the
routine's "don't spend long here" guidance.

**Secrets hygiene:** `.gitignore` still covers `.env`, `.venv/`, `venv/`.
No `.env` file present in the working tree. `git status --porcelain`
clean of any secret-like files.

**Conclusion:** codebase remains clean per Run 27's precedent — no audit-
sourced small fixes bundled this run. Proceeding straight to the selected
feature (correlation/Fisher-z post-hoc power — see
`.prism/research_2026-08-12-run28.md`).
