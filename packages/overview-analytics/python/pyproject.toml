[project]
name = "prism-overview-analytics"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pandas>=2.0,<3.0", "numpy>=1.24,<3.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
